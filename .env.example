# Glonetz staff frontend — copy to .env.local

# Backend local (npm run dev dans glonetzErpBackend, port 4000)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Render (hors reseau entreprise uniquement)
# NEXT_PUBLIC_API_BASE_URL=https://glonetzerpbackend-1.onrender.com

# mock | api
NEXT_PUBLIC_DATA_PROVIDER=api
